export interface Tcard {
  id: number;
  img: string;
  icon: JSX.Element;
  title: string;
  desc: string;
}
export interface Plan {
  id: number;
  title: string;
  plane: string;
  type: string;
  features: string[];
}
